import React from "react";

export default function Hero() {
    return (
        <section className="hero">
            <h1>OCR Web</h1>
            <p>Ứng dụng nhận diện văn bản từ hình ảnh bằng AI – nhanh, chính xác, dễ dùng</p>
            <a href="#ocr" className="btn mt-6 inline-block">Bắt đầu ngay</a>
        </section>
    );
}
